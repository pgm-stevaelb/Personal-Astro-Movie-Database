---
import "../styles/global.css";
import AppShell from "../components/AppShell";

const { title } = Astro.props;
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title ? `${title} · Serie Tracker` : "Serie Tracker"}</title>
  </head>
  <body>
    <AppShell client:load title={title}>
      <slot />
    </AppShell>
  </body>
</html>
