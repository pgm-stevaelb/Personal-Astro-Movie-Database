# Supabase schema

Run the SQL in `schema.sql` inside the Supabase SQL editor.

Steps:
1. Open your project in Supabase.
2. Go to SQL Editor.
3. Paste the contents of `schema.sql` and run.
4. Confirm tables and policies exist in Database > Tables.
